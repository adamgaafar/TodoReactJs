import { component$ } from "@builder.io/qwik";

export const Footer = component$(() => {
  return (
    <div class='bg-main absolute bottom-0 left-0 w-full text-white text-center py-2'>
      <p>copy write ©️ 2024 Programming was done by engineer Ibrahim Samir</p>
    </div>
  );
});
